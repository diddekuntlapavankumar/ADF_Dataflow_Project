{
	"name": "pipelinedailytrigger",
	"properties": {
		"annotations": [],
		"runtimeState": "Stopped",
		"pipelines": [
			{
				"pipelineReference": {
					"referenceName": "PL_Copy_pipelineparameter_sqldatabase_sqldatabase",
					"type": "PipelineReference"
				},
				"parameters": {
					"startdate": "@formatDateTime(addDays(trigger().scheduledTime,-1),'yyyy-MM-dd')",
					"enddate": "@formatDateTime(trigger().scheduledTime,'yyyy-MM-dd')"
				}
			},
			{
				"pipelineReference": {
					"referenceName": "pl_copy_pipelineparameter_datalake_sqldatabase",
					"type": "PipelineReference"
				},
				"parameters": {
					"startdate": "@formatDateTime(addDays(trigger().scheduledTime,-1),'yyyy-MM-dd')",
					"enddate": "@formatDateTime(trigger().scheduledTime,'yyyy-MM-dd')"
				}
			}
		],
		"type": "ScheduleTrigger",
		"typeProperties": {
			"recurrence": {
				"frequency": "Minute",
				"interval": 5,
				"startTime": "2026-05-06T16:30:00",
				"timeZone": "India Standard Time"
			}
		}
	}
}